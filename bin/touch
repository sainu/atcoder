#!/usr/bin/env ruby
# frozen_string_literal: true

filepath = ARGV[0]
unless File.exist?(filepath)
  File.open(filepath, 'w') do |f|
    template = <<-EOT
# frozen_string_literal: true

require 'bundler/setup'
require 'pry'

    EOT
    f.puts template
  end
end
